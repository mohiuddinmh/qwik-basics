import { component$ } from "@builder.io/qwik";

const EditInitiatives = component$(() => {
  return <>
    <h4>Add Initiative</h4>
    <input type="text" />
    <button>add</button>
  </>
})

export default EditInitiatives